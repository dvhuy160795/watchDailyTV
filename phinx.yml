paths:
    migrations: 'phinx/db/migrations'
    seeds: 'phinx/db/seeds'

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: mysql
        host: localhost
        name: watchtv
        user: root
        pass: ''
        port: 3306
        charset: utf8

    development:
        adapter: mysql
        host: localhost
        name: watchtv
        user: root
        pass: ''
        port: 3306
        charset: utf8

    testing:
        adapter: mysql
        host: localhost
        name: watchtv
        user: root
        pass: ''
        port: 3306
        charset: utf8

version_order: creation
